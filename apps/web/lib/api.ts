const BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:5000/api";
export type Analysis = { _id: string; fileName: string; targetRole: string; atsScore: number; roleMatch: number; summary: string; strengths: string[]; improvements: string[]; missingTechnicalSkills: string[]; missingSoftSkills: string[]; grammarFeedback: string[]; keywords: { word: string; present: boolean; importance: "high" | "medium" }[]; sectionScores: { section: string; score: number }[]; createdAt: string };
export async function api<T>(path: string, options: RequestInit = {}): Promise<T> { const token = typeof window !== "undefined" ? localStorage.getItem("resume_token") : null; const res = await fetch(`${BASE}${path}`, { ...options, headers: { ...(options.body instanceof FormData ? {} : { "Content-Type": "application/json" }), ...(token ? { Authorization: `Bearer ${token}` } : {}), ...options.headers } }); const data = await res.json(); if (!res.ok) throw new Error(data.message || "Request failed"); return data; }
